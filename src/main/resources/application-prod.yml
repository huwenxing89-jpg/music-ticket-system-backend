# 生产环境配置
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/music_ticket_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: musicapp
    password: Music@Ticket2024

  # Redis配置（生产环境暂时禁用）
  # redis:
  #   host: ${REDIS_HOST}
  #   port: 6379
  #   password: ${REDIS_PASSWORD}
  #   database: 0

# 日志配置
logging:
  level:
    com.music: info
    org.springframework: warn
  file:
    name: C:\inetpub\wwwroot\music-ticket-backend\logs\application.log

# Knife4j接口文档配置
knife4j:
  production: false

# 文件上传配置
file:
  upload-path: C:\inetpub\wwwroot\music-ticket-backend\uploads
  access-url: /files
